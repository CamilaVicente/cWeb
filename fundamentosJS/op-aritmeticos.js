const [ a ,b ,c ,d] = [3, 5, 1, 15]

const some = a + b + c + d; // unfix operador entre os numeros
const sub = d - b
const mult = a * b
const div = d / a
const mod = a % 2 // resto da divisão 


console.log(some, sub, mult, div, mod);



